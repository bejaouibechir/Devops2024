apiVersion: v1
kind: Service
metadata:
  name: sql-exporter
  namespace: flotte
spec:
  type: ClusterIP
  selector:
    app: sql-exporter
  ports:
    - name: http
      port: 9399
      targetPort: 9399

