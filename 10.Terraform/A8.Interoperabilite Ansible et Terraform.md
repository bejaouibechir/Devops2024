# Atelier : Ansible installe PostgreSQL, Terraform administre PostgreSQL

## 1. Contexte et objectif

Cet atelier montre la complémentarité entre **Ansible** et **Terraform** à travers un cas concret :

**Ansible** installe et configure **PostgreSQL** sur un serveur Ubuntu distant.

**Terraform** se connecte ensuite à PostgreSQL et administre les objets internes de la base :

**rôles**, **bases**, **schémas**, **extensions**, **privilèges**.

Le provider **cyrilgdn/postgresql** permet de gérer avec Terraform des objets PostgreSQL comme les **databases**, **extensions** et **roles**. Il est publié sur le Terraform Registry, et la version GitHub du projet indique une release récente **v1.26.0** publiée le **1 septembre 2025**. ([GitHub](https://github.com/cyrilgdn/terraform-provider-postgresql "GitHub - cyrilgdn/terraform-provider-postgresql: Terraform PostgreSQL provider · GitHub"))

## Message principal de l’atelier

```text
Ansible prépare le serveur.
Terraform administre PostgreSQL.
```

Terraform ne doit pas remplacer Ansible pour installer des paquets Linux, modifier des fichiers système ou gérer des services.

Ansible est mieux adapté pour :

**apt**, **systemd**, **postgresql.conf**, **pg_hba.conf**.

Terraform est mieux adapté pour :

**database**, **role**, **schema**, **grant**, **extension**.

---

# 2. Architecture de l’atelier

```text
Machine de contrôle Ubuntu
│
├── Ansible
│   ├── se connecte au serveur distant en SSH
│   ├── installe PostgreSQL
│   ├── configure postgresql.conf
│   ├── configure pg_hba.conf
│   └── redémarre PostgreSQL
│
└── Terraform
    ├── utilise le provider cyrilgdn/postgresql
    ├── se connecte au serveur PostgreSQL
    ├── crée un rôle applicatif
    ├── crée une base de données
    ├── crée un schéma
    └── attribue des privilèges
```

---

# 3. Prérequis

## Machines

```text
Machine de contrôle :
Ubuntu accessible en SSH, utilisée pour exécuter Ansible et Terraform

Serveur PostgreSQL :
Ubuntu accessible en SSH depuis la machine de contrôle

Dans le lab de validation :
Machine de contrôle : 192.168.100.54
Serveur PostgreSQL  : 192.168.100.54
Utilisateur SSH     : ubuntu
Clé privée SSH      : ~/.ssh/id_rsa
```

## Informations à adapter

Dans tout l’atelier, remplacez :

```text
IP_SERVEUR_POSTGRES
```

par l’adresse IP réelle du serveur PostgreSQL.

Dans ce lab :

```text
IP_SERVEUR_POSTGRES = 192.168.100.54
```

## Utilisateur SSH

L’atelier suppose que l’utilisateur distant est :

```text
ubuntu
```

et que la connexion SSH fonctionne avec une clé privée.

Test attendu depuis la machine de contrôle :

```bash
# Vérifie que la machine PostgreSQL est accessible en SSH.
ssh -i ~/.ssh/id_rsa ubuntu@192.168.100.54 "hostname"
```

Résultat attendu :

```text
Le nom de la machine distante s’affiche.
```

## Fichier d’inventaire Ansible

Ansible a besoin de savoir quelles machines gérer. Ce fichier s’appelle l’**inventaire**.

Vérifier ou créer le fichier `/etc/ansible/hosts` :

```bash
sudo nano /etc/ansible/hosts
```

Contenu à ajouter :

```ini
# Groupe contenant le serveur PostgreSQL de l’atelier.
[postgres_servers]
postgres_host ansible_host=192.168.100.54 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa
```

Explication des champs :

```text
[postgres_servers]            → nom du groupe Ansible
postgres_host                 → alias de la machine dans Ansible
ansible_host=192.168.100.54  → adresse IP réelle du serveur
ansible_user=ubuntu           → utilisateur SSH utilisé pour la connexion
ansible_ssh_private_key_file  → chemin vers la clé privée SSH
```

Adapter `ansible_host` à l’IP réelle du serveur PostgreSQL dans votre lab.

Vérifier que l’inventaire est correct :

```bash
# Teste la connexion Ansible vers le groupe postgres_servers.
ansible postgres_servers -m ping
```

Résultat attendu :

```text
postgres_host | SUCCESS => {
    "ping": "pong"
}
```

Si le résultat est `UNREACHABLE`, vérifier l’IP, l’utilisateur SSH et le chemin vers la clé privée.

---

## Installation des outils sur la machine de contrôle

Les deux machines étant vierges, installer d'abord Ansible, Terraform et le client PostgreSQL sur la machine de contrôle **192.168.100.54**.

Se connecter à la machine de contrôle :

```bash
ssh ubuntu@192.168.100.54
```

Installer Ansible et le client PostgreSQL :

```bash
sudo apt-get update
sudo apt-get install -y ansible postgresql-client curl gnupg lsb-release
```

Installer Terraform depuis le dépôt HashiCorp :

```bash
wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg > /dev/null

echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list

sudo apt-get update
sudo apt-get install -y terraform
```

Vérifier :

```bash
ansible --version
terraform version
```

Copier la clé privée SSH sur la machine de contrôle, par exemple dans :

```text
~/.ssh/id_rsa
```

Puis appliquer les permissions attendues :

```bash
chmod 600 ~/.ssh/id_rsa
```

---

# 4. Structure du projet

Créer le dossier principal :

```bash
# Crée le dossier racine de l'atelier.
mkdir -p ~/ansible-terraform-postgres

# Entre dans le dossier racine.
cd ~/ansible-terraform-postgres
```

Créer l’arborescence :

```bash
# Crée les dossiers Ansible et Terraform.
mkdir -p ansible terraform
```

Structure attendue :

```text
ansible-terraform-postgres/
│
├── ansible/
│   ├── inventory.ini
│   └── install-postgres.yml
│
└── terraform/
    └── main.tf
```

---

# 5. Phase 1 — Préparer Ansible

## Objectif

Déclarer la machine distante dans un inventaire Ansible.

---

## Étape 1 — Créer l’inventaire Ansible

Créer le fichier **ansible/inventory.ini** :

```ini
# Groupe Ansible contenant le serveur PostgreSQL.
[postgres_servers]

# Serveur PostgreSQL distant.
# Remplacer IP_SERVEUR_POSTGRES par l'adresse réelle.
postgres1 ansible_host=192.168.100.54 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa
```

## Étape 2 — Tester la connexion Ansible

Exécuter depuis le dossier **~/ansible-terraform-postgres** :

```bash
# Teste la connexion SSH via Ansible.
ansible -i ansible/inventory.ini postgres_servers -m ping
```

Résultat attendu :

```text
SUCCESS
```

## Point clé

**Ansible** utilise SSH pour configurer la machine distante.

Si cette étape échoue, il faut corriger SSH avant de continuer.

---

# 6. Phase 2 — Installer et configurer PostgreSQL avec Ansible

## Objectif

Ansible va :

**installer PostgreSQL**,  
**installer les dépendances utiles**,  
**activer le service**,  
**autoriser l’écoute réseau**,  
**autoriser la connexion distante**,  
**définir un mot de passe pour postgres**,  
**redémarrer PostgreSQL**.

Pour les modules Ansible liés à PostgreSQL, la documentation Ansible rappelle que les paquets **postgresql**, **libpq-dev** et **python3-psycopg2** sont nécessaires sur les hôtes Ubuntu quand on manipule PostgreSQL côté serveur. ([Ansible Documentation](https://docs.ansible.com/projects/ansible/latest/collections/community/postgresql/postgresql_set_module.html?utm_source=chatgpt.com "community.postgresql.postgresql_set module – Change a ..."))

---

## Étape 1 — Installer la collection Ansible PostgreSQL

Le module `community.postgresql.postgresql_user` est nécessaire pour gérer les rôles PostgreSQL sans conflit de privilèges.

Exécuter sur la **machine de contrôle** :

```bash
ansible-galaxy collection install community.postgresql
```

Vérifier l'installation :

```bash
ansible-galaxy collection list | grep postgresql
```

Résultat attendu :

```text
community.postgresql    x.x.x
```

---

## Étape 2 — Créer le playbook Ansible

Créer le fichier **ansible/install-postgres.yml** :

```yaml
---
# Ce playbook installe et configure PostgreSQL sur un serveur Ubuntu distant.
#
# Distinction importante des utilisateurs dans ce playbook :
#
#   ubuntu  → utilisateur Linux SSH défini dans l'inventaire (ansible_user=ubuntu)
#             Il se connecte via SSH et exécute les tâches avec sudo (become: true).
#             C'est l'utilisateur de la connexion SSH, pas un utilisateur PostgreSQL.
#
#   postgres → utilisateur Linux système créé automatiquement par l'installation de PostgreSQL.
#              PostgreSQL s'exécute sous cet utilisateur et accepte les connexions locales
#              via socket Unix sans mot de passe (authentification "peer" par défaut).
#              Pour certaines tâches PostgreSQL, Ansible doit agir en tant que cet utilisateur
#              avec become_user: postgres.
#
# Résumé :
#   become: true          → ubuntu devient root via sudo
#   become_user: postgres → root devient l'utilisateur système postgres
#                           pour accéder à PostgreSQL via socket Unix

- name: Installer et configurer PostgreSQL pour un atelier Terraform
  hosts: postgres_servers
  # become: true autorise Ansible à utiliser sudo pour les tâches système.
  # L'utilisateur SSH est "ubuntu" (défini dans l'inventaire).
  # Ubuntu n'a pas les droits root par défaut, sudo est donc nécessaire.
  become: true

  vars:
    # Mot de passe utilisé uniquement pour l'atelier.
    # En production, utiliser Ansible Vault.
    postgres_admin_password: "PostgresDemo123!"

    # Port PostgreSQL standard.
    postgres_port: 5432

  tasks:
    # Met à jour le cache APT pour obtenir la liste récente des paquets.
    - name: Mettre à jour le cache APT
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600

    # Installe PostgreSQL et les dépendances nécessaires.
    # acl est obligatoire pour que Ansible puisse utiliser become_user: postgres.
    # Sans acl, setfacl est absent (rc=127) et Ansible ne peut pas transférer
    # les droits sur ses fichiers temporaires à l'utilisateur postgres.
    - name: Installer PostgreSQL et les dépendances Python
      ansible.builtin.apt:
        name:
          - postgresql
          - postgresql-contrib
          - libpq-dev
          - python3-psycopg2
          - acl
        state: present

    # Active et démarre le service PostgreSQL.
    - name: Démarrer et activer PostgreSQL
      ansible.builtin.service:
        name: postgresql
        state: started
        enabled: true

    # Recherche le fichier postgresql.conf.
    # Sur Ubuntu, il se trouve généralement dans /etc/postgresql/<version>/main/postgresql.conf.
    - name: Trouver postgresql.conf
      ansible.builtin.find:
        paths: /etc/postgresql
        patterns: postgresql.conf
        recurse: true
      register: postgresql_conf_files

    # Recherche le fichier pg_hba.conf.
    # Ce fichier contrôle les connexions autorisées.
    - name: Trouver pg_hba.conf
      ansible.builtin.find:
        paths: /etc/postgresql
        patterns: pg_hba.conf
        recurse: true
      register: pg_hba_conf_files

    # Arrête le playbook si les fichiers de configuration sont introuvables.
    - name: Vérifier que les fichiers de configuration existent
      ansible.builtin.assert:
        that:
          - postgresql_conf_files.files | length > 0
          - pg_hba_conf_files.files | length > 0
        fail_msg: "Les fichiers postgresql.conf ou pg_hba.conf sont introuvables."

    # Stocke le chemin de postgresql.conf dans une variable temporaire.
    - name: Définir le chemin de postgresql.conf
      ansible.builtin.set_fact:
        postgresql_conf_path: "{{ postgresql_conf_files.files[0].path }}"

    # Stocke le chemin de pg_hba.conf dans une variable temporaire.
    - name: Définir le chemin de pg_hba.conf
      ansible.builtin.set_fact:
        pg_hba_conf_path: "{{ pg_hba_conf_files.files[0].path }}"

    # Configure PostgreSQL pour écouter sur toutes les interfaces réseau.
    # Pour un atelier, c'est simple.
    # En production, il faut limiter l'écoute réseau.
    - name: Autoriser PostgreSQL à écouter sur toutes les interfaces
      ansible.builtin.lineinfile:
        path: "{{ postgresql_conf_path }}"
        regexp: "^#?listen_addresses\\s*="
        line: "listen_addresses = '*'"
        backup: true
      notify: Redémarrer PostgreSQL

    # Configure PostgreSQL pour écouter sur le port 5432.
    - name: Configurer le port PostgreSQL
      ansible.builtin.lineinfile:
        path: "{{ postgresql_conf_path }}"
        regexp: "^#?port\\s*="
        line: "port = {{ postgres_port }}"
        backup: true
      notify: Redémarrer PostgreSQL

    # Autorise les connexions distantes en mot de passe.
    # Pour un atelier, 0.0.0.0/0 simplifie le test.
    # En production, remplacer par l'adresse IP exacte de la machine Terraform.
    - name: Autoriser les connexions distantes avec mot de passe
      ansible.builtin.blockinfile:
        path: "{{ pg_hba_conf_path }}"
        marker: "# {mark} ANSIBLE TERRAFORM POSTGRES LAB"
        block: |
          host    all             all             0.0.0.0/0               md5
        backup: true
      notify: Redémarrer PostgreSQL

    # Redémarre PostgreSQL maintenant pour que pg_hba.conf soit actif
    # avant de tenter la connexion pour changer le mot de passe.
    - name: Forcer le redémarrage PostgreSQL avant le changement de mot de passe
      ansible.builtin.service:
        name: postgresql
        state: restarted

    # Définit un mot de passe pour le rôle PostgreSQL "postgres".
    #
    # Pourquoi become_user: postgres ici ?
    # Par défaut, PostgreSQL sur Ubuntu autorise uniquement l'utilisateur système
    # "postgres" à se connecter sans mot de passe via la socket Unix (/var/run/postgresql).
    # Cette authentification s'appelle "peer" : PostgreSQL vérifie que l'utilisateur
    # Linux qui se connecte correspond au rôle PostgreSQL demandé.
    # L'utilisateur "ubuntu" n'a pas ce droit par défaut.
    # On doit donc temporairement agir en tant qu'utilisateur système "postgres"
    # pour exécuter cette tâche.
    #
    # Chaîne d'élévation :
    #   ubuntu (SSH) → root (sudo, become: true) → postgres (become_user: postgres)
    #
    # Le module community.postgresql.postgresql_user gère cette connexion
    # via socket Unix sans nécessiter de mot de passe PostgreSQL préexistant.
    - name: Définir le mot de passe du rôle postgres
      community.postgresql.postgresql_user:
        name: postgres
        password: "{{ postgres_admin_password }}"
        login_unix_socket: /var/run/postgresql
      become: true
      become_user: postgres
      no_log: true

    # Redémarre le service si une configuration a changé.
    - name: Forcer l'exécution des handlers maintenant
      ansible.builtin.meta: flush_handlers

    # Vérifie que PostgreSQL écoute bien sur le port attendu.
    - name: Vérifier que PostgreSQL écoute sur le port 5432
      ansible.builtin.command:
        cmd: ss -lnt
      register: listen_ports
      changed_when: false

    # Affiche les ports en écoute pour diagnostic.
    - name: Afficher les ports en écoute
      ansible.builtin.debug:
        var: listen_ports.stdout_lines

  handlers:
    # Handler exécuté uniquement si un fichier de configuration a changé.
    - name: Redémarrer PostgreSQL
      ansible.builtin.service:
        name: postgresql
        state: restarted
```

---

## Étape 3 — Exécuter le playbook

Exécuter depuis **~/ansible-terraform-postgres** :

```bash
# Lance l'installation et la configuration PostgreSQL.
ansible-playbook -i ansible/inventory.ini ansible/install-postgres.yml --become-password-file /dev/stdin
```

Remarque :

`--ask-become-pass` fonctionne aussi mais demande le mot de passe sudo de manière interactive :

```bash
ansible-playbook -i ansible/inventory.ini ansible/install-postgres.yml --ask-become-pass
```

Résultat attendu :

```text
PLAY RECAP
postgres1 : ok=... changed=... failed=0
```

## Point clé

À ce stade, **Ansible** a préparé le serveur PostgreSQL.

Terraform n’a encore rien fait.

---

# 7. Phase 3 — Vérifier PostgreSQL avant Terraform

## Objectif

Avant d’utiliser Terraform, on vérifie que PostgreSQL répond bien.

---

## Étape 1 — Vérifier le client PostgreSQL sur la machine de contrôle

Le client `psql` a été installé dans les prérequis.

```bash
psql --version
```

---

## Étape 2 — Tester la connexion distante

Exécuter depuis la machine de contrôle :

```bash
# Définit le mot de passe PostgreSQL pour le test psql.
export PGPASSWORD="PostgresDemo123!"

# Teste la connexion au serveur PostgreSQL distant.
psql -h 192.168.100.54 -U postgres -d postgres -c "SELECT version();"
```

Résultat attendu :

```text
PostgreSQL ...
```

## Si la connexion échoue

Vérifier :

```text
IP du serveur
port 5432 ouvert côté sécurité réseau
service postgresql démarré
listen_addresses dans postgresql.conf
règle pg_hba.conf
mot de passe postgres
```

Sur le serveur distant :

```bash
# Vérifie le service PostgreSQL.
# systemctl status ne nécessite pas sudo.
ssh ubuntu@$IP_SERVEUR_POSTGRES "systemctl status postgresql --no-pager"

# Vérifie que PostgreSQL écoute sur le port 5432.
# ss -lnt (sans p) ne nécessite pas sudo.
ssh ubuntu@$IP_SERVEUR_POSTGRES "ss -lnt | grep 5432"
```

Attention : toujours utiliser `$IP_SERVEUR_POSTGRES` avec le signe `$` pour que le shell développe la variable. Sans `$`, SSH interprète `IP_SERVEUR_POSTGRES` comme un nom d'hôte littéral et échoue avec "Temporary failure in name resolution".

Si sudo est nécessaire pour une commande, ajouter `-t` pour allouer un terminal interactif :

```bash
ssh -t ubuntu@$IP_SERVEUR_POSTGRES "sudo journalctl -u postgresql --no-pager | tail -20"
```

---

# 8. Phase 4 — Terraform administre PostgreSQL

## Objectif

Terraform va créer :

**un rôle applicatif**,  
**une base de données**,  
**un schéma**,  
**une extension**,  
**des privilèges**.

Le provider PostgreSQL est conçu pour gérer des objets PostgreSQL avec Terraform, notamment des bases, rôles et extensions. ([GitHub](https://github.com/cyrilgdn/terraform-provider-postgresql "GitHub - cyrilgdn/terraform-provider-postgresql: Terraform PostgreSQL provider · GitHub"))

---

## Étape 1 — Créer le fichier Terraform

Créer le fichier **terraform/main.tf** :

```hcl
# Le block terraform configure Terraform lui-même.
terraform {
  required_providers {
    postgresql = {
      source  = "cyrilgdn/postgresql"
      version = "~> 1.26"
    }
  }
}

variable "postgres_admin_password" {
  description = "Mot de passe du rôle PostgreSQL postgres."
  type        = string
  sensitive   = true
}

variable "app_user_password" {
  description = "Mot de passe du rôle applicatif créé par Terraform."
  type        = string
  sensitive   = true
}

# Le provider PostgreSQL configure la connexion au serveur PostgreSQL.
provider "postgresql" {
  host            = "192.168.100.54"
  port            = 5432
  database        = "postgres"
  username        = "postgres"
  password        = var.postgres_admin_password
  sslmode         = "disable"
  connect_timeout = 15
}

# Crée un rôle applicatif.
# login = true permet au rôle de se connecter.
resource "postgresql_role" "app_user" {
  name     = "app_user_tf"
  login    = true
  password = var.app_user_password
}

# Crée une base de données applicative.
# owner donne la propriété de la base au rôle applicatif.
resource "postgresql_database" "app_db" {
  name  = "app_db_tf"
  owner = postgresql_role.app_user.name
}

# Crée une extension dans la base applicative.
# uuid-ossp est utile pour générer des UUID côté PostgreSQL.
resource "postgresql_extension" "uuid_ossp" {
  name     = "uuid-ossp"
  database = postgresql_database.app_db.name
}

# Crée un schéma dans la base applicative.
resource "postgresql_schema" "app_schema" {
  name     = "app"
  database = postgresql_database.app_db.name
  owner    = postgresql_role.app_user.name
}

# Donne des privilèges au rôle applicatif sur le schéma.
resource "postgresql_grant" "app_schema_usage" {
  database    = postgresql_database.app_db.name
  role        = postgresql_role.app_user.name
  schema      = postgresql_schema.app_schema.name
  object_type = "schema"
  privileges  = ["USAGE", "CREATE"]
}
```

## Attention

Les mots de passe ne sont pas écrits directement dans **main.tf**.  
Ils sont fournis à Terraform avec des variables sensibles.

Pour cet atelier, définir les variables d’environnement avant `terraform plan` ou `terraform apply` :

```bash
export TF_VAR_postgres_admin_password="PostgresDemo123!"
export TF_VAR_app_user_password="AppUserDemo123!"
```

Attention : même avec `sensitive = true`, certains secrets peuvent exister dans le state selon les ressources et providers.  
En production, utiliser Vault, un secret manager ou des variables protégées CI/CD.

---

# 9. Phase 5 — Cycle Terraform complet

## Objectif

Exécuter le workflow Terraform complet :

```text
init → validate → plan → apply → state list → show
```

---

## Étape 1 — Se placer dans le dossier Terraform

```bash
# Entre dans le dossier Terraform.
cd ~/ansible-terraform-postgres/terraform
```

---

## Étape 2 — Initialiser

```bash
# Fournit les secrets à Terraform pour cette session shell.
export TF_VAR_postgres_admin_password="PostgresDemo123!"
export TF_VAR_app_user_password="AppUserDemo123!"

# Initialise Terraform et télécharge le provider PostgreSQL.
terraform init
```

Résultat attendu :

```text
Terraform has been successfully initialized!
```

## Explication

**init** télécharge le provider **cyrilgdn/postgresql**.

---

## Étape 3 — Valider

```bash
# Vérifie la syntaxe et la cohérence du fichier Terraform.
terraform validate
```

Résultat attendu :

```text
Success! The configuration is valid.
```

## Explication

**validate** vérifie le code Terraform avant l’exécution.

---

## Étape 4 — Prévisualiser

```bash
# Affiche ce que Terraform va créer dans PostgreSQL.
terraform plan
```

Résultat attendu :

```text
Plan: 5 to add, 0 to change, 0 to destroy.
```

Validé avec Terraform v1.15.5 et le provider `cyrilgdn/postgresql` v1.26.0 : le plan initial annonce bien 5 créations.

## Explication

**plan** montre ce qui sera créé dans PostgreSQL :

**role**, **database**, **extension**, **schema**, **grant**.

---

## Étape 5 — Appliquer

```bash
# Applique le plan Terraform.
terraform apply
```

Quand Terraform demande confirmation :

```text
Enter a value:
```

Taper :

```text
yes
```

Résultat attendu :

```text
Apply complete!
```

---

## Étape 6 — Lister les ressources Terraform

```bash
# Liste les objets PostgreSQL suivis par Terraform.
terraform state list
```

Résultat attendu :

```text
postgresql_database.app_db
postgresql_extension.uuid_ossp
postgresql_grant.app_schema_usage
postgresql_role.app_user
postgresql_schema.app_schema
```

## Point clé

Terraform ne suit pas le paquet PostgreSQL installé par Ansible.

Terraform suit uniquement les objets qu’il a créés dans PostgreSQL.

---

## Étape 7 — Inspecter le state

```bash
# Affiche le state complet.
terraform show
```

Inspecter une ressource précise :

```bash
# Affiche les détails du rôle PostgreSQL.
terraform state show postgresql_role.app_user

# Affiche les détails de la base de données.
terraform state show postgresql_database.app_db
```

---

# 10. Phase 6 — Vérifier directement dans PostgreSQL

## Objectif

Confirmer que les objets Terraform existent réellement côté PostgreSQL.

---

## Étape 1 — Vérifier les bases

Depuis la machine de contrôle :

```bash
# Utilise le mot de passe admin PostgreSQL.
export PGPASSWORD="PostgresDemo123!"

# Liste les bases contenant le nom de l'atelier.
psql -h 192.168.100.54 -U postgres -d postgres -c "\l"
```

Résultat attendu :

```text
app_db_tf
```

---

## Étape 2 — Vérifier les rôles

```bash
# Liste les rôles PostgreSQL.
psql -h 192.168.100.54 -U postgres -d postgres -c "\du"
```

Résultat attendu :

```text
app_user_tf
```

---

## Étape 3 — Vérifier le schéma

```bash
# Liste les schémas dans la base app_db_tf.
psql -h 192.168.100.54 -U postgres -d app_db_tf -c "\dn"
```

Résultat attendu :

```text
app
```

---

## Étape 4 — Vérifier l’extension

```bash
# Liste les extensions installées dans app_db_tf.
psql -h 192.168.100.54 -U postgres -d app_db_tf -c "\dx"
```

Résultat attendu :

```text
uuid-ossp
```

---

## Étape 5 — Tester la connexion avec le rôle applicatif

```bash
# Définit le mot de passe du rôle applicatif.
export PGPASSWORD="AppUserDemo123!"

# Teste la connexion avec le rôle créé par Terraform.
psql -h 192.168.100.54 -U app_user_tf -d app_db_tf -c "SELECT current_user, current_database();"
```

Résultat attendu :

```text
current_user  | app_user_tf
current_database | app_db_tf
```

---

# 11. Phase 7 — Comprendre la séparation Ansible et Terraform

## Ce qu’Ansible a géré

```text
Installation du paquet postgresql
Installation des dépendances Python
Démarrage du service PostgreSQL
Activation du service au démarrage
Modification de postgresql.conf
Modification de pg_hba.conf
Définition du mot de passe postgres
Redémarrage du service
```

## Ce que Terraform a géré

```text
postgresql_role.app_user
postgresql_database.app_db
postgresql_extension.uuid_ossp
postgresql_schema.app_schema
postgresql_grant.app_schema_usage
```

## Différence essentielle

**Ansible** agit sur le **serveur Linux**.

**Terraform** agit sur les **objets PostgreSQL**.

---

# 12. Phase 8 — Détection de changement Terraform

## Objectif

Modifier un objet PostgreSQL et observer le plan Terraform.

---

## Étape 1 — Remettre le state dans un état propre

Si `terraform state list` retourne du vide, le state a été perdu. Les objets existent dans PostgreSQL mais Terraform ne les connaît plus. Il faut nettoyer PostgreSQL et recréer les ressources pour que le state soit cohérent.

### 1a — Supprimer les objets PostgreSQL existants

Se connecter au serveur PostgreSQL et supprimer dans l'ordre :

```bash
ssh -t ubuntu@$IP_SERVEUR_POSTGRES "sudo -u postgres psql -c 'DROP DATABASE IF EXISTS app_db_tf;'"
ssh -t ubuntu@$IP_SERVEUR_POSTGRES "sudo -u postgres psql -c 'DROP ROLE IF EXISTS app_user_tf;'"
```

Vérifier que les objets n'existent plus :

```bash
ssh -t ubuntu@$IP_SERVEUR_POSTGRES "sudo -u postgres psql -c '\l' | grep app_db_tf || echo 'base absente'"
ssh -t ubuntu@$IP_SERVEUR_POSTGRES "sudo -u postgres psql -c '\du' | grep app_user_tf || echo 'rôle absent'"
```

Résultat attendu :

```text
base absente
rôle absent
```

### 1b — Recréer les ressources avec Terraform

Depuis le dossier **~/ansible-terraform-postgres/terraform** :

```bash
terraform apply -auto-approve
```

Résultat attendu :

```text
Apply complete! Resources: 4 added, 0 changed, 0 destroyed.
```

### 1c — Vérifier le state

```bash
terraform state list
```

Résultat attendu :

```text
postgresql_database.app_db
postgresql_extension.uuid
postgresql_role.app_user
postgresql_schema.app_schema
```

Le state est maintenant cohérent. On peut passer à la démonstration de modification.

---

## Étape 2 — Vérifier la valeur actuelle avant modification

```bash
terraform state show postgresql_database.app_db
```

Relever la valeur actuelle de `connection_limit`. Elle doit être à `-1` (illimité, valeur par défaut).

---

## Étape 3 — Modifier la base de données

Dans le fichier **terraform/main.tf**, modifier uniquement `connection_limit` dans la ressource existante **postgresql_database.app_db** :

```hcl
# Modification : connection_limit passe de -1 (illimité) à 10.
# C'est la seule ligne qui change par rapport à la Phase 4.
resource "postgresql_database" "app_db" {
  name              = "app_db_tf"
  owner             = postgresql_role.app_user.name
  connection_limit  = 10
  allow_connections = true
}
```

Cette modification est choisie délibérément : elle modifie un attribut en place sans détruire ni recréer la base, ce qui produit un plan `~` (update) et non `+/-` (replace).

---

## Étape 4 — Prévisualiser

```bash
terraform plan
```

Résultat attendu :

```text
  ~ resource "postgresql_database" "app_db" {
        name             = "app_db_tf"
      ~ connection_limit = -1 -> 10
    }

Plan: 0 to add, 1 to change, 0 to destroy.
```

Le symbole `~` signifie modification en place. Aucune destruction de la base.

---

## Étape 5 — Appliquer

```bash
terraform apply
```

Taper :

```text
yes
```

---

## Étape 6 — Inspecter

```bash
terraform state show postgresql_database.app_db
```

Vérifier que `connection_limit` vaut maintenant `10`.

## Point clé

Terraform compare :

**main.tf**  
avec  
**terraform.tfstate**  
et avec l’état réel du serveur PostgreSQL lorsque le provider rafraîchit les informations.

---

# 13. Phase 9 — Destruction des objets PostgreSQL avec Terraform

## Objectif

Supprimer uniquement les objets PostgreSQL gérés par Terraform.

---

## Étape 1 — Prévisualiser la destruction

Depuis le dossier **terraform** :

```bash
# Affiche ce que Terraform supprimerait.
terraform plan -destroy
```

Résultat attendu :

```text
Plan: 0 to add, 0 to change, 5 to destroy.
```

## Étape 2 — Détruire

```bash
# Supprime les objets PostgreSQL gérés par Terraform.
terraform destroy
```

Taper :

```text
yes
```

Résultat attendu :

```text
Destroy complete!
```

## Attention

Cette commande supprime les objets PostgreSQL créés par Terraform.

Elle ne désinstalle pas PostgreSQL.

Elle ne restaure pas automatiquement **postgresql.conf** ou **pg_hba.conf**.

Ces éléments relèvent d’**Ansible**.

---

# 14. Phase 10 — Nettoyage optionnel avec Ansible

## Objectif

Désinstaller PostgreSQL si l’atelier est terminé.

Cette phase est optionnelle.

---

## Étape 1 — Créer le playbook de nettoyage

Créer le fichier **ansible/remove-postgres.yml** :

```yaml
---
# Ce playbook désinstalle PostgreSQL du serveur Ubuntu distant.
- name: Désinstaller PostgreSQL après l'atelier
  hosts: postgres_servers
  become: true

  tasks:
    # Arrête le service PostgreSQL si présent.
    - name: Arrêter PostgreSQL
      ansible.builtin.service:
        name: postgresql
        state: stopped
      ignore_errors: true

    # Désinstalle les paquets PostgreSQL installés pour l'atelier.
    - name: Désinstaller PostgreSQL et les dépendances associées
      ansible.builtin.apt:
        name:
          - postgresql
          - postgresql-contrib
          - libpq-dev
          - python3-psycopg2
        state: absent
        purge: true
        autoremove: true

    # Supprime les répertoires de configuration et de données.
    # Attention : cette opération efface les données PostgreSQL locales.
    - name: Supprimer les répertoires PostgreSQL restants
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /etc/postgresql
        - /var/lib/postgresql
        - /var/log/postgresql
```

## Étape 2 — Exécuter le nettoyage

Depuis le dossier **~/ansible-terraform-postgres** :

```bash
# Désinstalle PostgreSQL du serveur distant.
ansible-playbook -i ansible/inventory.ini ansible/remove-postgres.yml
```

## Attention

Ce nettoyage supprime PostgreSQL et ses données locales.

Ne jamais exécuter ce playbook sur un serveur contenant des données importantes.

---

# 15. Scénarios à exploiter pendant l’atelier

## Scénario 1 — Ansible seul

Chaîne :

```text
ssh → ansible ping → ansible-playbook → systemctl status
```

Objectif :

Montrer qu’**Ansible** prépare l’environnement système.

---

## Scénario 2 — Terraform seul après préparation

Chaîne :

```text
terraform init → validate → plan → apply → state list → show
```

Objectif :

Montrer que **Terraform** administre les objets PostgreSQL.

---

## Scénario 3 — Vérification croisée

Chaîne :

```text
terraform state list → psql \l → psql \du → psql \dn
```

Objectif :

Comparer la vision **Terraform state** avec la réalité **PostgreSQL**.

---

## Scénario 4 — Destruction maîtrisée

Chaîne :

```text
terraform plan -destroy → terraform destroy → psql \l
```

Objectif :

Montrer que **Terraform détruit seulement ce qu’il connaît**.

---

## Scénario 5 — Nettoyage système avec Ansible

Chaîne :

```text
terraform destroy → ansible-playbook remove-postgres.yml
```

Objectif :

Montrer que le nettoyage applicatif et le nettoyage système sont deux responsabilités différentes.

---

# 16. Questions à poser aux participants

## Question 1

Pourquoi n’installe-t-on pas PostgreSQL directement avec le provider Terraform PostgreSQL ?

Réponse attendue :

Parce que le provider PostgreSQL administre un serveur PostgreSQL déjà accessible.  
Il ne gère pas les paquets Linux ni les services système.

## Question 2

Pourquoi Ansible est-il adapté à l’installation ?

Réponse attendue :

Parce qu’Ansible sait gérer les **paquets**, **fichiers**, **services**, **redémarrages** et **configuration système**.

## Question 3

Pourquoi Terraform est-il adapté à la création des objets PostgreSQL ?

Réponse attendue :

Parce que Terraform garde un **state** des objets déclarés et peut planifier leur création, modification ou destruction.

## Question 4

Que se passe-t-il si on lance **terraform destroy** ?

Réponse attendue :

Terraform supprime les objets PostgreSQL qu’il gère, mais ne désinstalle pas PostgreSQL du serveur.

## Question 5

Pourquoi faut-il éviter de mettre les mots de passe dans **main.tf** ?

Réponse attendue :

Parce que le fichier peut être versionné, partagé ou lu par erreur.  
Les secrets doivent être externalisés.

---

# 17. Points clés à retenir

**Ansible** prépare le serveur.

**Terraform** administre les objets déclaratifs.

**Le provider PostgreSQL** n’installe pas PostgreSQL.

**Le provider PostgreSQL** gère des objets PostgreSQL comme les bases, rôles, schémas, extensions et droits.

**terraform state list** montre ce que Terraform suit.

**psql** montre ce qui existe réellement dans PostgreSQL.

**terraform destroy** ne remplace pas un playbook de désinstallation.

---

# 18. Conclusion

Cet atelier montre une séparation saine entre deux outils complémentaires :

```text
Ansible = installation et configuration système
Terraform = gestion déclarative des ressources PostgreSQL
```

C’est une approche beaucoup plus réaliste qu’un atelier où Terraform ferait tout avec **remote-exec**.

La bonne logique DevOps est :

```text
Préparer le serveur avec Ansible.
Administrer les ressources avec Terraform.
Vérifier avec psql.
Nettoyer chaque couche avec le bon outil.
```

# 
