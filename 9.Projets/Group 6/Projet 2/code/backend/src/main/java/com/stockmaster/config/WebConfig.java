package com.stockmaster.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // CORS géré par Nginx — aucune config CORS côté applicatif
}
